{
  "devDependencies": {
    "cssnano": "^4.0.5",
    "pug": "^2.0.3",
    "workbox-cli": "^3.4.1"
  },
  "dependencies": {
    "autoprefixer": "^9.1.0",
    "postcss-modules": "^1.3.2",
    "wordcloud": "^1.1.0"
  },
  "scripts": {
    "start": "npx parcel index.pug",
    "prebuild": "rm -rf dist",
    "build": "NODE_ENV=production npx parcel build index.pug",
    "postbuild": "npx workbox-cli generateSW",
    "publish": "netlify deploy"
  }
}
